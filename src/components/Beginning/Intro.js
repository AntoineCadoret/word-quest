import React, { useState } from 'react';

const Intro = (props) => {
  
  return (
    <div>
        <h2>intro</h2> 
        <button onClick={props.begin}>former les equipes</button>
    </div>
  );
}

export default Intro;